

## 兩項更新：About 文案 + PDF 檔案替換

### 1. About 頁面開場白替換（`src/pages/About.tsx`）

將第 137-145 行的三段文字全部替換為兩段新文案：

- **第一段**（含 `<br /><br />` 斷行）：「我在科技製造業待了 8+ 年，做過創新型筆電、服務型機器人、無人機、醫療設備等複雜系統。多數專案都是新領域，剛開始都很模糊，大家不確定該往哪走」
- **第二段**：「我的角色是幫團隊把問題問清楚、把選項收斂到可以執行的程度，用研究讓決策有依據而不是憑感覺。這樣做能讓大家少繞路、少做白工，也降低專案方向大改的風險」

### 2. 替換 Resume PDF 檔案

將上傳的新版 PDF 複製到 `public/Resume_Tai-Yun_Chen_Jack_tw.pdf`，覆蓋舊檔。下載路徑不變，不需改動任何程式碼。

### 不變動的部分

- `src/pages/Resume.tsx`：不做任何修改
- `AboutPreview.tsx`：首頁預覽區文案不變
- 下載邏輯、SEO meta：維持不變

### 修改檔案總覽

| 檔案 | 變更 |
|------|------|
| `src/pages/About.tsx` | 替換開場白三段為兩段新文案 |
| `public/Resume_Tai-Yun_Chen_Jack_tw.pdf` | 替換為新版 PDF |

