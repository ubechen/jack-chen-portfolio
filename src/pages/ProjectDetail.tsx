import ProjectDetailV2 from "@/pages/ProjectDetailV2";

const ProjectDetail = () => {
  // Always render V2 version with quick navigation
  return <ProjectDetailV2 />;
};

export default ProjectDetail;
